# Evidence run log (redacted)

本文件可提交；**不含** DOM dump。真实 JSON 见 gitignored `runs/*.json`。

| UTC 时间戳文件 | status | homeOk | conversationCovered | conversationOk | 备注 |
|----------------|--------|--------|---------------------|----------------|------|
| probe-session-20260720-200214Z.json | ran | true | **true** | **true** | 本机 CDP 操控：composer 输入 `probe-conversation-marker` 后进入会话；`inConversation=true` |

勾选 RELEASE-EVIDENCE **conversation** 的前提已满足（本机留痕，勿提交 JSON）。
